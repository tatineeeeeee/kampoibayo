/** Maximum character length for a single SMS message */
export const SMS_MAX_LENGTH = 160;

/** Delay between consecutive SMS sends to avoid carrier rate limiting */
export const SMS_SEND_DELAY_MS = 1000;
